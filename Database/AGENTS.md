# 数据库

## 范围
- 仅包含 SQL 脚本；本目录不改应用代码。
- 架构变更需与 `App.config` 中连接字符串名 `mysql` 协同。

## 变更规范
- 尽量保持脚本幂等；对手工步骤添加注释说明。
- 对破坏性操作（DROP/TRUNCATE）注明影响与备份要求。
- 如涉及报表变更，需同步更新相关 `.rdlc` 文件。

## 验证
- 提交前先在当前架构上验证脚本可执行。

